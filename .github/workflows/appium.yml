name: Appium Android Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Enable KVM hardware acceleration
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      # Use Appium 2 stable — NOT @next (v3 beta breaks driver installs)
      - name: Install Appium 2 + UiAutomator2 driver
        run: |
          npm install -g appium@latest
          appium --version
          appium driver install uiautomator2
          appium driver list --installed

      - name: Install project dependencies
        run: npm install --legacy-peer-deps

      - name: Verify APK exists
        run: |
          if [ ! -f "app/app.apk" ]; then
            echo "APK NOT FOUND at app/app.apk"
            exit 1
          fi
          echo "APK found: $(ls -lh app/app.apk)"

      - name: Run tests on emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          profile: pixel_6
          target: google_apis
          avd-name: test_avd
          force-avd-creation: false
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
          disable-animations: true
          script: |
            # Start Appium 2 in background
            appium --port 4723 --log appium.log --log-level info &
            APPIUM_PID=$!

            # Wait for Appium
            TIMEOUT=60
            ELAPSED=0
            until curl -sf http://127.0.0.1:4723/status > /dev/null 2>&1; do
              sleep 3
              ELAPSED=$((ELAPSED + 3))
              echo "Waiting for Appium... ${ELAPSED}s"
              if [ $ELAPSED -ge $TIMEOUT ]; then
                cat appium.log || true
                exit 1
              fi
            done
            echo "Appium is ready!"

            # Run tests
            node test_sample.js
            TEST_EXIT=$?

            echo "=== appium.log ==="
            cat appium.log || true

            kill $APPIUM_PID 2>/dev/null || true
            exit $TEST_EXIT
